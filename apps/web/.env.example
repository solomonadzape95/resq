NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
# Map is OpenStreetMap (free, no key). No env var needed.

# ElevenLabs Conversational AI — agent ID for the /call browser screen.
# Find this in dashboard.elevenlabs.io → Conversational AI → Agents.
# Leave blank to render the /call page in "demo unavailable" mode.
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=
